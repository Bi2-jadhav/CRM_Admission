package com.adminService.AdminService.Repository;

import com.adminService.AdminService.Entity.LeadList;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LeadListRepository extends JpaRepository<LeadList, Long> {
}
